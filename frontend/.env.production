# Same-origin in production: the backend serves the React build,
# so API calls use relative /api/... paths.
VITE_API_BASE_URL="https://vt-phpapp-02-due7bkame8hadxeb.australiaeast-01.azurewebsites.net/api-docs.php"
