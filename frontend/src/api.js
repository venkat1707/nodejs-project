export const apiBaseUrl = import.meta.env.VITE_API_BASE_URL ?? "http://localhost:4000";

// URL of the PHP documentation page (served by a PHP-capable server, e.g. `php -S localhost:8000`).
export const docsUrl = import.meta.env.VITE_DOCS_URL ?? "http://localhost:8000/api-docs.php";
