PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=sales_db
# Set DB_SSL=false only for a local MySQL that does not use TLS.
# Azure MySQL / most managed MySQL require SSL, so keep it true (default).
DB_SSL=true
