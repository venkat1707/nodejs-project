# Dependencies
node_modules/

# Environment / secrets
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# Build output (regenerated by `npm run build:deploy` — not committed).
# NOTE: the PHP docs SOURCE is frontend/public/api-docs.php and IS tracked;
# only its BUILT copy under backend/public is ignored below.
frontend/dist/
backend/public/
dist/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
